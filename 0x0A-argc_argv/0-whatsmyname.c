#include <stdio.h>

/**
 * main - Program prints the name.
 * @argc: number of arguments supplied to the program.
 * @argv: An array of pointer to the arguments.
 * Return: Always 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
